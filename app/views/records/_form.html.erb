<%= simple_form_for @record, url: project_item_records_path(@project, @item), method: :post do |f| %>

  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :expenditure %>
    <%= f.input :comments %>
    <%= f.hidden_field  :item_id, :value => params[:item_id] %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
