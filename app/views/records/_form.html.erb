<%= simple_form_for(@record) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :expenditure %>
    <%= f.association :Item %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
