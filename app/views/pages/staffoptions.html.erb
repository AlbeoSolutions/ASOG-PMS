<div class="main-wrapper">

  <h1 class="project-heading">Manage staff accounts: </h1>

  <table class="table staff-table">
    <thead>
      <th> Staff name </th>
      <th> Contract expiration Date</th>
      <th> Options </th>
    </thead>

    <tbody>
        <% @staffs.each do |staff| %>
          <tr>
            <td> <%= staff %> </td>
            <td> <%= staff.contract_expiration_date.strftime('%b' +" " +'%d' +", "+ '%Y') %> </td>
            <td> <%= link_to staff_registration_path(staff_id: staff), :confirm => "Are you sure?", :method => :delete do %>
              <span class="glyphicon glyphicon-remove"></span>
              <% end %>
          </tr>
        <% end %>
    </tbody>
  </table>

</div>
